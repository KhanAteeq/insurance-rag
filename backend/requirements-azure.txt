# ============================================================================
# Slim requirements for AZURE / cloud deployment (BACKEND_MODE=azure).
# Excludes torch + sentence-transformers (~2 GB) because embeddings come from
# Azure OpenAI. Used by App Service so the build is fast and small.
# For LOCAL/offline mode, use requirements.txt instead.
# ============================================================================
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
PyMuPDF==1.24.10
chromadb==1.0.21
# Azure App Service ships an old system sqlite3; pysqlite3-binary provides a
# modern build that our app/__init__.py swaps in before ChromaDB imports.
pysqlite3-binary==0.5.4
ollama==0.3.3
httpx==0.27.2
openai>=1.55,<2
numpy==1.26.4
